# Changelog

## Unreleased

- Add `public/_headers` with security headers for Cloudflare Pages (HSTS, CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy).
- Replace SVG social-share image with a 1200×630 PNG (`og-default.png`) so Facebook/LinkedIn/X render link previews; add `og:image:width`/`height` meta.
